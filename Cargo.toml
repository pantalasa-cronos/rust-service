[package]
name = "rust-service"
version = "0.1.0"
edition = "2021"
rust-version = "1.70.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
assert_cmd = "2.0"
